import { traderDataAccessDataAccess } from './trader-data-access-data-access';

describe('traderDataAccessDataAccess', () => {
  it('should work', () => {
    expect(traderDataAccessDataAccess()).toEqual(
      'trader-data-access-data-access'
    );
  });
});
