import { traderUtilityDataAccess } from './trader-utility-data-access';

describe('traderUtilityDataAccess', () => {
  it('should work', () => {
    expect(traderUtilityDataAccess()).toEqual('trader-utility-data-access');
  });
});
